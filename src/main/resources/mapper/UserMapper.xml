<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zp.chapter01.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.zp.chapter01.pojo.dataobject.UserDo">
        <!-- 自增ID -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <!-- 姓名  -->
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <!-- 密码 -->
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <!-- 生日 -->
        <result column="birthday" property="birthday" jdbcType="DATE"/>
        <!-- 年龄 -->
        <result column="age" property="age" jdbcType="INTEGER"/>
        <!-- 性别 -->
        <result column="sex" property="sex" jdbcType="VARCHAR"/>
        <!-- 电话 -->
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <!-- 地址 -->
        <result column="address" property="address" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 通用查询结果列名 -->
    <sql id="Base_Column_Name">
        id,username,password,birthday,age,sex,phone,address
    </sql>

    <!-- 动态条件 -->
    <sql id="BaseCondition">
        <where>
            <if test="id != null">
                AND id = #{id,jdbcType=INTEGER}
            </if>
            <if test="username != null">
                AND username = #{username,jdbc=VACHAR}
            </if>
            <if test="birthday != null">
                AND birthday = #{birthday,jdbcType=DATE}
            </if>
            <if test="age != null">
                AND age = #{age,jdbc=INTEGER}
            </if>
            <if test="sex != null">
                AND sex = #{sex,jdbc=VACHAR}
            </if>
            <if test="phone != null">
                AND phone = #{phone,jdbc=VACHAR}
            </if>
            <if test="address != null">
                AND address = #{address,jdbc=VACHAR}
            </if>
        </where>
    </sql>

    <!-- 根据动态条件查询语句(根据 BaseCondition 查询语句) -->
    <sql id="selectByBaseCondition">
        SELECT
        <include refid="Base_Column_Name" />
        FROM user
        <include refid="BaseCondition" />
    </sql>

    <select id="findAll" resultType="com.zp.chapter01.pojo.dataobject.UserDo">
        SELECT * FROM user
    </select>
</mapper>